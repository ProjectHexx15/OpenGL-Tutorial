#version 330 core
out vec4 FragColor; // output a color to the framebuffer

void main()
{
	FragColor = vec4(1.0f, 0.2f, 0.6f, 1.0f) 
	// the colour values are in the range of 0.0 to 1.0, so this is a pink colour, end number is alpha value

}	